# RailMate UI / UX 设计文档

## 1. 设计目标

RailMate 的 UI/UX 设计遵循三个核心原则：

- **像聊天一样自然**：让用户先说需求，而不是先填表。
- **像工具一样可靠**：关键车次信息必须卡片化、结构化、可点击。
- **像出行助手一样贴身**：在关键时刻主动给用户下一步行动。

### 1.1 视觉语言（设计 tokens 摘要）

- **圆角**：全局卡片/输入/按钮统一偏大圆角（约 `12px～18px` 阶梯），避免「方盒子堆砌」的廉价感。
- **阴影**：轻阴影分层（`shadow-xs` / `shadow-card`），悬停略抬升，强调可点击性。
- **对话卡片**：车次/中转卡片强调 **车次号、时刻、价格** 层级（字号与字重高于次要信息）；列表头 **路线文案** 与真实结果站名聚合一致（避免「搜广州却写死广州南」的违和）。
- **深色模式**：对话框、检票凭证等使用 **elevated surface** 与分隔线透明度调和，保证对比度与阅读舒适。

产品采用“双入口设计”：

- `CUI`：对话式输入，负责承接模糊需求、情绪化表达和连续追问。
- `GUI`：卡片式界面，负责承接结构化信息、比较决策和操作执行。

## 2. CUI 对话流设计

## 2.1 标准对话流

```text
用户输入需求
   ↓
系统理解意图
   ↓
补齐缺失信息（如出发地/日期）
   ↓
调用工具查票/查中转/查详情
   ↓
返回自然语言总结 + 卡片
   ↓
用户点击卡片继续查看 / 购票 / 改条件
```

## 2.2 对话流一：直达查票

```text
用户：明天从广州去长沙最快的高铁
系统：已为你查到明天广州南 → 长沙南的直达高铁，
      推荐最早可出发方案如下：
      [车次卡片1] [车次卡片2] [更多]
用户：第二趟多少钱？
系统：二等座 ¥xxx，一等座 ¥xxx，是否查看完整时刻表？
```

### 设计要点

- 首轮回答不堆砌过多数据，只给 2~3 个最优结果。
- 第二轮再展开票价、停靠站、替代方案。

## 2.3 对话流二：语音输入

```text
[用户点击麦克风]
系统状态：正在听...
用户：后天下午回南昌，便宜一点
系统：已识别为“后天下午去南昌，价格优先”。
      如果你默认从景德镇北出发，我现在就帮你查。
      [确认并查询]
```

### 设计要点

- 先展示识别结果，让用户建立信任感。
- 对语义不完整时，用一句确认话术快速补齐。

## 2.4 对话流三：沿途游玩

```text
用户：我想从广州去西安，顺路在长沙玩一天
系统：收到，这是“分段行程 + 沿途停留”场景。
      我会先查 广州→长沙，再查次日 长沙→西安。
      [分段行程卡1]
      [分段行程卡2]
      [长沙停留建议]
```

### 设计要点

- 明确告诉用户系统理解的是“旅行规划”而不是普通换乘。
- 将“交通段”和“停留建议”分开排版。

## 2.5 对话流四：买长乘短 / 风险提醒

```text
用户：上海到南京票没了，买到徐州能不能提前下？
系统：这个场景涉及买长乘短规则，我可以先帮你做风险评估：
      1. 该做法可能涉及检票与席位规则风险
      2. 更推荐的替代方案是分段购票/补票/换站出发
      3. 我可以继续帮你比较三种方案的时间与成本
      [查看替代方案]
```

### 设计要点

- 不直接回答“可以/不可以”，而是给规则感 + 替代感。
- 体现“负责任的 AI”。

## 3. GUI 卡片原型描述

## 3.1 首页布局原型

```text
+----------------------------------------------------------------------------------+
| RailMate Logo | 会话历史 | 新建对话 | 当前位置：景德镇北 | 偏好：平衡 | 设置        |
+----------------------+---------------------------------------------+-------------+
| 最近对话 / 常用路线   |                                             | 用户上下文   |
| - 去长沙              |               对话主区域                    | - 当前城市   |
| - 回广州              |  ┌───────────────────────────────────────┐  | - 推荐车站   |
| - 去北京出差          |  | 用户消息                              |  | - 常用线路   |
|                      |  | AI回答 + 车次卡片 + 快捷操作            |  | - 规划模式   |
|                      |  └───────────────────────────────────────┘  |             |
|                      |  [语音按钮] [输入框....................] [发送] |             |
+----------------------------------------------------------------------------------+
```

### 描述

- 左栏强调历史和快捷路线，帮助高频用户“秒回访”。
- 中间是产品主舞台，卡片和机器人欢迎态都放在这里。
- 右栏用于上下文强化，向评委展示“这不是单轮问答，而是有记忆的 Agent”。

## 3.2 查票结果卡片原型

```text
┌──────────────────────────────────────────────────────────────┐
│ G1408  高铁   [最早发车]                           余票充足  │
│ 广州南 07:32  ───── 2小时38分 ─────>  长沙南 10:10 │
│ 最低价：¥314   一等座：¥504   商务座：¥936         │
│ [查看详情] [对比下一趟] [查中转] [演示购票]          │
└──────────────────────────────────────────────────────────────┘
```

### 描述

- 第一行给用户建立“选择理由”。
- 第二行突出时间与效率。
- 第三行突出价格与购买意图。
- 第四行承接下一步操作。

## 3.3 沿途游玩卡片原型

```text
┌──────────────────────────────────────────────────────────────┐
│ 方案 A：广州南 → 长沙南（当天） → 西安北（次日）           │
│ 第一段：G6112 08:15-10:43                                   │
│ 停留：长沙 24小时，可推荐岳麓山 / 五一广场 / 夜宵路线       │
│ 第二段：G856 09:20-15:48                                    │
│ 总预算：¥718   总体验：铁路+城市探索                        │
│ [看长沙攻略] [替换停留时长] [继续购票]                      │
└──────────────────────────────────────────────────────────────┘
```

## 3.4 车次详情页原型

```text
┌──────────────────────────────────────────────────────────────┐
│ ← 返回搜索        G1408    高铁                      2026-03-21 │
├──────────────────────────────────────────────────────────────┤
│ 广州南 07:32 -------------------------------> 长沙南 10:10    │
│ 共 7 站                                                          │
├──────────────────────────────────────────────────────────────┤
│ 票价卡片： 商务座 | 一等座 | 二等座 | 无座                      │
├──────────────────────────────────────────────────────────────┤
│ 时刻表时间轴                                                    │
│ ● 广州南 07:32                                                  │
│ ● 韶关   08:11                                                  │
│ ● 郴州西 08:56                                                  │
│ ● 长沙南 10:10                                                  │
├──────────────────────────────────────────────────────────────┤
│ [演示购票] [加入行程] [分享给朋友]                             │
└──────────────────────────────────────────────────────────────┘
```

## 3.5 设置页原型

```text
+-------------------------------------------------------------+
| 当前定位：景德镇 | 偏好：平衡 | 模式：沿途游玩               |
| [IP定位] [GPS定位] [手动设置城市/车站]                      |
|                                                             |
| 出行偏好：                                                  |
| ( ) 更快到达   ( ) 更省钱   (●) 平衡                        |
|                                                             |
| 规划模式：                                                  |
| (●) 高效赶路   ( ) 铁路运转   ( ) 沿途游玩                  |
|                                                             |
| 12306 账号：                                                |
| [生成二维码登录]                                            |
+-------------------------------------------------------------+
```

## 4. GUI 与 CUI 协同原则

### 原则一：对话负责理解，卡片负责落地

- 模糊需求用聊天承接。
- 明确结果用卡片承接。
- 关键操作一定落到按钮，而不是继续让用户口头表达。

### 原则二：每个回答都要有“下一步”

例如：

- 查看详情
- 查中转
- 改日期
- 演示购票
- 加入行程

### 原则三：先结论，后解释

AI 回答应先给“推荐哪一趟 / 为什么”，再给详细说明，否则用户会觉得啰嗦。

### 原则四：协议不入眼

- `::actions::[...]` 等机器协议必须从界面剥离，用户只能看到**自然语言 + 按钮**。
- 助手勿引导「详见上方卡片」——卡片位于消息**下方**，避免方位错误。

## 5. 关键交互细节

## 5.1 语音按钮

- 默认状态为麦克风图标
- 录音中变为脉冲态
- 顶部浮层实时显示“正在听...”或临时转写结果
- 识别失败时显示短错误提示

## 5.2 结果卡片动效

- 新卡片从下方轻微上浮
- 多卡片按 40ms 左右延迟依次出现
- 不使用夸张动画，强调“稳、快、专业”

## 5.3 空结果反馈

空结果不能只说“没有车次”，而要给下一步：

- 改查明天
- 改查中转
- 换附近站点
- 改价格优先 / 时间优先

## 5.4 高风险场景提示

对买长乘短、跨站补票、异常站名、国外定位等情况，要：

- 用黄/橙色提示卡显示风险
- 不打断用户，但提醒规则边界
- 给合规替代操作

## 5.5 快捷回复按钮（Assistant Quick Replies）

- **布局**：移动端优先纵向或可换行；宽屏可单行等分，保证触控热区。
- **互斥**：当同轮已展示车次/中转**工具卡片**时，默认不再叠加快捷按钮（由前后端策略约束），避免「一排卡片 + 一排按钮」信息过载。
- **解析**：支持 `::actions::` 出现在正文**中间**；展示文案为去协议后的合并句子。

## 5.6 查票表单

- **日期行**：竖屏与横屏均保持 **日期选择器 +「今天」「明天」** 同一行（`minmax(0,1fr) + auto` 网格），避免第二行孤岛按钮。
- **清除按钮**：发到站两侧 **常驻清除**，与 Autocomplete 内置清除二选一（`disableClearable`），禁止「双叉」。

## 5.7 演示购票 / 成功弹窗

- **底部主副按钮**：纵向满宽堆叠，`DialogActions` 重置 MUI 默认「第二个按钮 margin-left」以免第二颗变窄；统一 `minHeight`、圆角与字重。
- **状态**：提交中主按钮展示 `CircularProgress` 尺寸与图标一致，避免高度跳动。

## 5.8 行程与检票凭证

- **二维码**：编码为完整检票 URL；**立即检票**与扫码共享同一后端状态。
- **列表**：已检票行程 **隐藏退票**，与演示规则一致。

## 6. UniApp 适配建议

- 首页保留单列布局，左侧历史与右侧上下文收纳到抽屉中。
- 输入区固定底部，支持语音与快捷短语。
- 结果卡片为纵向滑动列表，更适合移动场景。
- 重要按钮加大点击热区，适应移动端单手操作。

## 7. 结论

RailMate 的 UI/UX 不是传统票务系统的“表单 + 列表”，而是“对话理解 + 卡片决策 + 连续操作”的 AI 原生交互范式。CUI 帮用户表达意图，GUI 帮用户快速比较与执行，两者结合后，产品既有科技感，也有铁路出行场景真正需要的效率感。

**答辩演示顺序与评委话术**见 `docx/00_答辩与演示导读.md`。
